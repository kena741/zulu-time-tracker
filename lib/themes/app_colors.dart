import 'package:flutter/material.dart';

/// Royal green on deep charcoal — Work Diary / Upwork-style dashboard look.
abstract class AppColors {
  /// Bright accent for links, keys, and activity segments on dark surfaces.
  static const royalGreen = Color(0xFF00E676);

  /// Deep green for light-mode chrome and secondary emphasis.
  static const primary = Color(0xFF006E3C);

  static const surfaceLight = Color(0xFFF6F6F6);
  static const surfaceDark = Color(0xFF121212);
  static const surfaceRaised = Color(0xFF1E1E1E);
  static const outlineMuted = Color(0xFFBDBDBD);
}
